/**
 * Class-name merge helper.
 *
 * Canonical implementation lives in `@/lib/utils`; this module re-exports it so
 * components can import the more intuitive `@/utils/cn` path.
 */
export { cn } from '@/lib/utils';
